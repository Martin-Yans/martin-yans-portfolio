image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - find . -maxdepth 1 -type f -exec cp {} public/ \;
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH